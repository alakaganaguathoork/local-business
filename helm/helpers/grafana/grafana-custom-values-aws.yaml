adminPassword: admin

ingress:
  enabled: true
  ingressClassName: alb
  path: /
  extraPaths:
    - path: /
      pathType: Prefix
      backend:
        service:
          name: grafana
          port:
            number: 80
